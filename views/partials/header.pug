header.header
    .header__column.logo
        a(href=routes.home)
            i.fas.fa-camera-retro
            |MISSED
    
    .header__column.search
        form(method="get", action=routes.search)
            input(type="text", placeholder="원하시는 키워드로 검색해보세요 !", name="term")

    .header__column
        ul
            li 
                a(href="")
                    i.fas.fa-th-large
            li
                a(href="")
                    i.fas.fa-folder
                
            if loggedUser
                li
                    a(href=routes.user+'/'+loggedUser.id) 내 프로필
                li
                    a(href=routes.logout) 로그아웃
                li
                    a.uploadBtn(href=routes.photo+routes.uploadPhoto) 업로드
            else
                li
                    a(href=routes.login) 로그인
                li
                    a.registerBtn(href=routes.join) 회원가입