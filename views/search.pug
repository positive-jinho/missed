extends layouts/main
include mixins/photoGrid

block content
    .page-title '#{term}' 검색결과
    
    if photos[0].length !== 0
        +photoGrid(photos)
    else
        span 검색 결과가 없습니다. 😥